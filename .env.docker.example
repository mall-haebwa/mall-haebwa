# ========================================
# Docker Compose 환경 변수 설정 파일
# ========================================
# 이 파일은 docker-compose.yml에서 사용하는 환경변수를 정의합니다.
# 실제 프로덕션 환경에서는 이 파일을 복사하여 사용하세요.
#
# 사용법:
# 1. 이 파일을 .env.docker로 복사: cp .env.docker.example .env.docker
# 2. 필요한 값들을 실제 환경에 맞게 수정
# 3. docker-compose up 실행 시 자동으로 로드됨

# ========================================
# MongoDB 설정
# ========================================
# MongoDB 연결 URL (Docker 내부 네트워크 사용)
MONGODB_URL=mongodb://mongodb:27017

# MongoDB 데이터베이스 이름
MONGODB_DB_NAME=ecommerce_ai
MONGODB_PRODUCT_DB_NAME=naver_shopping

# ========================================
# Backend 설정
# ========================================
# 디버그 모드 (개발: True, 프로덕션: False)
DEBUG=True

# CORS 허용 오리진 (쉼표로 구분)
# 개발 환경
CORS_ORIGINS=http://localhost:3000,http://localhost:5173
# 프로덕션 환경 (YOUR_EC2_IP를 실제 IP로 변경)
# CORS_ORIGINS=http://YOUR_EC2_IP:3000,http://YOUR_EC2_IP:8000

# ========================================
# Frontend 설정
# ========================================
# API 서버 URL
# 개발 환경
VITE_API_URL=http://localhost:8000
# 프로덕션 환경 (YOUR_EC2_IP를 실제 IP로 변경)
# VITE_API_URL=http://YOUR_EC2_IP:8000

# ========================================
# JWT 설정 (추후 사용)
# ========================================
# SECRET_KEY=your-secret-key-here
# ALGORITHM=HS256
# ACCESS_TOKEN_EXPIRE_MINUTES=30

# ========================================
# AI/LLM 설정 (추후 사용)
# ========================================
# OPENAI_API_KEY=your-openai-key
# ANTHROPIC_API_KEY=your-anthropic-key
